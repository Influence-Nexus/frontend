# Contributing

## Coverage

Generated assets are excluded from coverage reports to keep metrics focused on source code. The configuration ignores directories such as `frontend/build/`, `frontend/android/`, `frontend/ios/`, and other generated outputs.

Run `npm run test:coverage` from the `frontend` directory to regenerate the coverage report.